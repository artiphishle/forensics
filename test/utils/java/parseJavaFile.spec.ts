import assert from 'node:assert';
import test, { describe } from 'node:test';

describe('[parseJavaFile]', () => {
  // Test:
  test.skip('Correctly parses .java file', () => {
    assert.strictEqual(true, false);
  });
});
