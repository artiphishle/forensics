import assert from 'node:assert';
import test, { describe } from 'node:test';

describe('[filterByPackagePrefix]', () => {
  // Test:
  test.skip('Filters packages by "currentPackage." prefix', () => {
    assert.strictEqual(true, false);
  });
});
