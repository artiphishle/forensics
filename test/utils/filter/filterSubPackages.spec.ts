import assert from 'node:assert';
import test, { describe } from 'node:test';

describe('[filterSubPackages]', () => {
  // Test:
  test.skip('Sub packages are filtered correctly', () => {
    assert.strictEqual(true, false);
  });
});
