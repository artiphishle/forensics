import assert from 'node:assert';
import test, { describe } from 'node:test';

describe('[generateSequenceFromProject]', () => {
  // Test:
  test.skip('Correct sequence diagram is generated', () => {
    assert.strictEqual(true, false);
  });
});
