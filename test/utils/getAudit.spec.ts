import assert from 'node:assert';
import test, { describe } from 'node:test';

describe('[getAudit]', () => {
  // Test:
  test.skip('Correct audit is generated', () => {
    assert.strictEqual(true, false);
  });
});
